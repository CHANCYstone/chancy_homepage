---
import Base from '../../layouts/Base.astro';
import Header from '../../components/layout/Header.astro';
import Footer from '../../components/layout/Footer.astro';
import PixelCard from '../../components/ui/PixelCard.astro';
import PixelBadge from '../../components/ui/PixelBadge.astro';
import { getCollection } from 'astro:content';

const allProjects = await getCollection('projects');
const sorted = allProjects.sort((a, b) => b.data.date.localeCompare(a.data.date));

const categories = ['all', 'principles', 'writing', 'quant', 'code'] as const;

const badgeColors: Record<string, 'blue' | 'red' | 'green' | 'yellow'> = {
  code: 'red',
  principles: 'green',
  writing: 'blue',
  quant: 'yellow',
};
---

<Base title="Projects">
  <Header />
  <main>
    <h1 class="text-[2rem] text-[var(--color-primary)] mb-[var(--spacing-xl)]">
      PROJECTS
    </h1>

    <div class="flex flex-wrap gap-[var(--spacing-sm)] mb-[var(--spacing-xl)]">
      {categories.map(cat => (
        <button
          data-filter={cat}
          class="filter-btn bg-[var(--color-neutral)] text-[var(--color-on-neutral)] border-4 border-[var(--color-border)] px-[var(--spacing-md)] py-[var(--spacing-sm)] text-[0.625rem] shadow-[var(--shadow-pixel-sm)] cursor-pointer hover:bg-[var(--color-accent)] hover:text-[var(--color-on-neutral)]"
        >
          {cat.toUpperCase()}
        </button>
      ))}
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-[var(--spacing-md)]" id="projects-grid">
      {sorted.map(project => (
        <div data-category={project.data.category} class="project-item">
          <PixelCard href={`/projects/${project.id}`}>
            <div class="mb-[var(--spacing-sm)]">
              <PixelBadge color={badgeColors[project.data.category]}>
                {project.data.category.toUpperCase()}
              </PixelBadge>
            </div>
            <h3 class="text-[0.75rem] text-[var(--color-on-neutral)] mb-[var(--spacing-sm)]">
              {project.data.title}
            </h3>
            <p class="text-[0.5rem] text-[var(--color-muted)] mb-[var(--spacing-xs)]">
              {project.data.date}
            </p>
            <p class="text-[0.625rem] text-[var(--color-on-neutral)] line-clamp-2">
              {project.data.summary}
            </p>
          </PixelCard>
        </div>
      ))}
    </div>
  </main>
  <Footer />
</Base>

<script>
  document.querySelectorAll('.filter-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      const filter = (btn as HTMLElement).dataset.filter;
      document.querySelectorAll('.project-item').forEach(item => {
        const el = item as HTMLElement;
        if (filter === 'all' || el.dataset.category === filter) {
          el.style.display = '';
        } else {
          el.style.display = 'none';
        }
      });
      document.querySelectorAll('.filter-btn').forEach(b => {
        (b as HTMLElement).style.backgroundColor = '';
      });
      (btn as HTMLElement).style.backgroundColor = 'var(--color-accent)';
    });
  });
</script>
